######################################################
#
# ${package.name}
#
# ${copyright}
#
######################################################

[install]
is_configured = true
state = enabled
build = ${version.build}

[launcher]
author = Splunk Inc.
version = ${version.major}.${version.minor}.${version.revision}
description = Splunk_TA_mytest

[ui]
is_visible = true
label = Splunk_TA_mytest
docs_section_override=AddOns:released

[package]
id = ${package.name}
